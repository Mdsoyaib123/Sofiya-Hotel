import { useLoaderData } from "react-router-dom";

const RoomDetelis = () => {
    const data = useLoaderData()
    console.log(data);
     return (
        <div>
            
        </div>
    );
};

export default RoomDetelis;